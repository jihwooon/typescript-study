import styled from "styled-components";

const Pagination = () => {
  return (
    <>
      <PaginationStyle>
        <h1>Pagination</h1>
      </PaginationStyle>
    </>
  );
};

const PaginationStyle = styled.div``;

export default Pagination;
